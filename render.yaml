services:
  - type: web
    name: spire-of-the-archon
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
